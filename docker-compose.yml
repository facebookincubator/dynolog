services:
  docker-dynolog:
    container_name: dynolog
    command: --use_prometheus true
    network_mode: host
    build:
      context: ./
      target: docker_dynolog
      args:
        - USE_PROMETHEUS=ON
    volumes:
      - /proc:/proc





